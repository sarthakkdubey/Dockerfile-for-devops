# Java Application Dockerization

This project contains a Docker setup for running a Java application using Docker containers.  
The Dockerfile is designed for Java applications packaged as JAR files and follows a simple production-style structure.

---

## Prerequisites

Make sure the following tools are installed on your system:

- Java 17 or later
- Maven or Gradle
- Docker

You can verify Docker installation using:

```bash
docker --version
